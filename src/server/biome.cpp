#include "biome.hpp"

namespace col {

	/*auto BIOME_NAMES = vector<string>{
		"none",
		"tundra",
		"prairie",
		"plains",
		"desert",
		"savannah",
		"grassland",
		"marsh",
		"swamp",
		"arctic"
	};	
		
	string const& Biome::get_name() const {
		auto sid = get_serial_id();
		if (0 <= sid and sid < BIOME_NAMES.size()) {
			return BIOME_NAMES[sid];
		}
		else {
			return "<unknown-item-name>";			
		}
	}

	
	Biome Biome::by_name(string const& name) {
		for (int i = 0; i < BIOME_NAMES.size(); ++i) {
			if (n == name) {
				return Biome(i);
			}
		}
		return BiomeNone;
	}*/

}
